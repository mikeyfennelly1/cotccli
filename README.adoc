= Desktop application for sysinfo in y2/b3 project

This application returns system information as JSON, based on the `Message` struct in `sysinfo/sysinfo_reading.go`, on a 1-second incremental basis.